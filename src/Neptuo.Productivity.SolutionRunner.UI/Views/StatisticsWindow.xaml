<Window x:Class="Neptuo.Productivity.SolutionRunner.Views.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:dd="clr-namespace:Neptuo.Productivity.SolutionRunner.UI.DesignData"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DataContext="{x:Static dd:ViewModelLocator.Statistics}"
        xmlns:converters="clr-namespace:Neptuo.Productivity.SolutionRunner.Views.Converters"
        xmlns:cm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        Title="Solution runner - Statistics" Width="500" MinHeight="300" SizeToContent="Height" 
        Background="{StaticResource BackgroundBrush}" WindowStartupLocation="CenterOwner" WindowStyle="None"
        PreviewKeyDown="OnPreviewKeyDown" PreviewMouseDown="OnPreviewMouseDown">
    <Window.Resources>
        <converters:PathConverter x:Key="PathConverter" />

        <CollectionViewSource x:Key="ApplicationsSource" Source="{Binding Applications}">
            <CollectionViewSource.SortDescriptions>
                <cm:SortDescription PropertyName="Count" Direction="Descending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
        <CollectionViewSource x:Key="FilesSource" Source="{Binding Files}">
            <CollectionViewSource.SortDescriptions>
                <cm:SortDescription PropertyName="Count" Direction="Descending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </Window.Resources>
    <Border Style="{StaticResource WindowBorderStyle}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ItemsControl ItemsSource="{Binding Source={StaticResource ApplicationsSource}}" Grid.Column="0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Path, Converter={StaticResource PathConverter}}" Foreground="{Binding ColorBrush}" />
                            <TextBlock Text=" " />
                            <TextBlock Text="{Binding Count}" Foreground="{Binding ColorBrush}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <ItemsControl ItemsSource="{Binding Source={StaticResource FilesSource}}" Grid.Column="1">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Path, Converter={StaticResource PathConverter}}" Foreground="{Binding ColorBrush}" />
                            <TextBlock Text=" " />
                            <TextBlock Text="{Binding Count}" Foreground="{Binding ColorBrush}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</Window>
