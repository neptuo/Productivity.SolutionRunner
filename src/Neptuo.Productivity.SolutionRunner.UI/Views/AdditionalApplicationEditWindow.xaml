<Window x:Class="Neptuo.Productivity.SolutionRunner.Views.AdditionalApplicationEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" 
        xmlns:dd="clr-namespace:Neptuo.Productivity.SolutionRunner.UI.DesignData" d:DataContext="{x:Static dd:ViewModelLocator.AdditionalApplication}"
        xmlns:ui="clr-namespace:Neptuo.Productivity.SolutionRunner.Views.Controls"
        xmlns:ds="clr-namespace:Neptuo.Productivity.SolutionRunner.Views.DataSources"
        xmlns:converters="clr-namespace:Neptuo.Productivity.SolutionRunner.Services.Converters"
        Title="Solution runner - Additional application" Width="450" SizeToContent="Height" ResizeMode="NoResize" 
        ShowInTaskbar="False" Background="{StaticResource BackgroundBrush}" WindowStartupLocation="CenterScreen" WindowStyle="None"
        PreviewKeyDown="OnPreviewKeyDown" PreviewMouseDown="OnPreviewMouseDown">
    <Window.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource DescriptionStyle}">
            <Setter Property="Padding" Value="2" />
        </Style>
    </Window.Resources>
    <Border Style="{StaticResource WindowBorderStyle}">
        <DockPanel Style="{StaticResource RootPanelStyle}">
            <Grid DockPanel.Dock="Bottom">
                <StackPanel HorizontalAlignment="Right" Style="{StaticResource ActionBarStyle}">
                    <Button Content="Apply" Command="{Binding SaveCommand}" IsDefault="True" />
                    <Button x:Name="btnCancel" Click="btnCancel_Click" Content="Cancel" IsCancel="True" />
                </StackPanel>
            </Grid>
            <StackPanel>
                <DockPanel>
                    <Image Source="{Binding Icon}" Width="32" Margin="5,0,15,0" DockPanel.Dock="Left" />
                    <StackPanel Style="{StaticResource EditorPanelStyle}">
                        <Grid>
                            <Label Content="Name:" Target="{Binding ElementName=tbxName}" />
                            <TextBlock Text="If not set, automatically taken from the path" HorizontalAlignment="Right" />
                        </Grid>
                        <TextBox x:Name="tbxName" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0" KeyUp="tbxName_KeyUp" />
                    </StackPanel>
                </DockPanel>
                <StackPanel Style="{StaticResource EditorPanelStyle}">
                    <Grid>
                        <Label Content="Path:" Target="{Binding ElementName=tbxName}" />
                        <TextBlock Text="Full path to additional application" HorizontalAlignment="Right" />
                    </Grid>
                    <TextBox x:Name="tbxPath" Text="{Binding Path, UpdateSourceTrigger=PropertyChanged}" Margin="0" />
                </StackPanel>
                <StackPanel Style="{StaticResource EditorPanelStyle}">
                    <Grid>
                        <Label Content="Arguments:" Target="{Binding ElementName=tbxArguments}" />
                        <TextBlock Text="If to set, {FilePath} is used" HorizontalAlignment="Right" />
                    </Grid>
                    <TextBox x:Name="tbxArguments" Text="{Binding Arguments, UpdateSourceTrigger=PropertyChanged}" Margin="0" />
                    <TextBlock TextWrapping="Wrap">
                        Here you can define additional application arguments. By default the only passed argument is a path to the selected file.
                        Value can contain dynamic blocks that are replaced by values from selected file.
                    </TextBlock>
                    <TextBlock TextWrapping="Wrap">
                        {FilePath} is replaced with a full path to the selected file.
                    </TextBlock>
                    <TextBlock TextWrapping="Wrap">
                        {DirectoryPath} is replaced with a full path to the directory of the selected file.
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </DockPanel>
    </Border>
</Window>
